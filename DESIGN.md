# daybook 基本設計

2026-08-01 の設計会話で確定した方針の記録。実装前の一次設計であり、
実装で得た知見によって改訂される。

## ゴール

- SharedPreferences のドロップイン置き換えになれる軽量 KV ストア
- 同期読み出し（Flow / coroutine を要求しない）
- プロセスクラッシュ・電源断に対する耐障害性
- マルチプロセス対応（既存勢の弱点であり、本プロジェクトの差別化点）
- SharedPreferences との**相互**マイグレーション（導入も撤退も一方通行にしない）
- デフォルトでリアクティビティを確保: 更新内容（キー + 新値）を受け取れる変更リスナーを
  コアプリミティブとして持つ。Flow 等の高級 API は後からリスナーの上に構築できる
- 純 Kotlin/JVM。ネイティブコード（C++/JNI）を持たない

## 非ゴール

- SQL 的なクエリ・トランザクション（それは Room の領分）
- 「絶対に失えない」データの保証（fsync ポリシーで緩和はするが、
  金銭情報等は Room / SQLite を使うべき）
- 大容量データ。想定は設定値・フラグ・小さな状態の集合（〜数千キー）

## アーキテクチャ

### 読み出し

- 初回オープン時にジャーナルをリプレイして `ConcurrentHashMap` に展開
- 以後の読み出しはすべてメモリアクセス。ディスク IO なし、同期 API のみ
- SharedPreferences の「初回ロード完了までメインスレッドブロック」問題は、
  オープンを明示的な suspend/バックグラウンド初期化にすることで API 上回避可能にする
  （同期オープンも提供するが、ドキュメントで初期化タイミングを制御させる）

### 書き込み

- シリアライズ済みエントリをジャーナル末尾に追記
- ファイル先頭に `[magic][version]` ヘッダ（将来のフォーマット進化の逃げ道。
  magic 不一致・未知バージョンは切り捨てで「復旧」せず例外にする — 別物ファイルを黙って壊さない）
- エントリ形式: `[length][payload][CRC32]`（リプレイ時に長さと CRC で検証。
  CRC は length + payload に掛け、長さフィールドの破損も検出する）
- ジャーナル層は KV 非依存のバイトレコードログ。KV 操作へのエンコードは上位層の責務
  （compaction・世代切替と直交させるため）
- fsync はポリシー選択制:
  - `SYNC`: 追記ごとに fsync。遅いが電源断まで耐える
  - `ASYNC`(デフォルト): OS のページキャッシュに任せる。プロセスクラッシュには耐える
- SharedPreferences の `QueuedWork` 的なライフサイクル同期化は**持たない**。
  ANR の根本原因だったため、書き込み完了をライフサイクルに紐付けない

### 耐障害性

- リプレイ時、CRC 不一致 or 長さ不整合を検出したら、そのエントリ以降（壊れたテール）を
  切り捨てて最後の正常状態に復旧する
- SQLite 級のリカバリロジックは持たない。「追記ログの末尾切り捨て」だけで完結させる

### マルチプロセス

追記ジャーナルをそのままプロセス間の変更配信チャネルとして使う:

- 各プロセスは「自分がどこまでリプレイしたか」のオフセットを保持
- `FileObserver`（inotify）でジャーナルの成長を検知し、差分だけリプレイして
  自プロセスのキャッシュに適用（全量再読み込みなし）
- 書き込み排他は二段構え:
  - プロセス内: ミューテックス（`FileLock` は同一 JVM 内で reentrant でないため）
  - プロセス間: `FileLock`（追記区間のみ）
- 既知の制約: inotify 通知は非同期のため「書いた直後に別プロセスで読むと古い値」の
  ウィンドウが原理的に残る。強一貫性が必要な読み出し向けに、ジャーナル末尾を確認してから
  読む API（`readFresh` 等）を逃げ道として用意する

実装方針の詳細（2026-08-02 の設計会話で確定）:

- マルチプロセス機構は open 時の opt-in（multiProcess フラグ）。単一プロセス利用者に
  FileLock syscall と監視スレッドのコストを払わせない
- プロセス間ロックの対象は、世代とともに rename されるジャーナルファイルではなく
  固定名 `<name>.lock` の専用ファイル。ロック順序は「プロセス内 writeLock →
  プロセス間 FileLock」で固定する
- FileLock は同一 JVM 内での重複取得が OverlappingFileLockException になるため、
  「1 JVM 内で 2 store を開いて 2 プロセスを模す」JVM テストに向けてロック層を
  インターフェースで抽象化する。実 FileLock の挙動は Instrumentation テストで検証する
- 成長・世代切替の検知は watcher 抽象の背後に置く。Android 実装は FileObserver
  （JVM で実行不可のためカバレッジ除外 + Instrumentation テスト）、JVM テストは
  手動トリガ実装を注入する
- 書き込みプロトコル: プロセス間ロック取得 → 世代番号の再確認（他プロセスの
  compaction 検知。変わっていれば開き直し）→ ジャーナル末尾までキャッチアップ → 追記。
  自プロセスの追記はリプレイ済みオフセットも進めるため、watcher 経由の差分リプレイと
  二重適用にならない。multiProcess モードではオープン時の復旧（テール切り捨て）も
  プロセス間ロック下で行う
- 差分リードで見つかる不完全なテールは、破損ではなく他プロセスの書き込み途中とみなす。
  切り捨てず完全レコード境界まで読んで次の成長を待つ。テールの切り捨ては
  書き込みロック保持下のオープン時復旧だけの権利
- 世代切替をまたぐ通知: compaction はスナップショットの末尾に境界マーカーレコードを
  書く。遅れて新世代を開き直したプロセスは、マーカーまでは通知せずに状態を再構築して
  自キャッシュとの差分だけを (key, newValue) で通知し、マーカー以降は通常の
  操作ベース通知に戻る。「同値 Put も通知する」原則の破れを切替の見逃し分の
  状態差分に閉じ込めるための裁定
- readFresh: ジャーナルの世代と末尾を確認し、遅れていれば同期キャッチアップしてから
  読む強一貫読み出し

### Compaction

**世代方式**を採用（ロックで全プロセスを止める方式より検証しやすいため）:

- ジャーナルが閾値を超えたら、現在のメモリ状態を新世代ファイルに書き出し、
  アトミック rename で切り替える
- ファイル名に世代番号を含める。他プロセスは世代番号の変化を検知したら
  新世代ファイルを開き直す
- 「プロセス A が compaction 中にプロセス B が追記」の競合が実装難度の本体。
  テスト用フックで compaction を任意の位置で一時停止できる仕掛けを本体に入れ、
  競合を決定的に踏ませてテストする

実装で確定した詳細（2026-08-02）:

- 「一時ファイルの内容 fsync → rename 発行 → 旧世代の削除発行」の順序を守る。
  「世代ファイルがないのに一時ファイルがある」状態は旧世代の削除がディスクに届いた後 —
  つまり一時ファイルの内容が完全に永続化された後 — にしか起こりえないため、
  このときに限り一時ファイルを正式な世代として採用できる
- ただしこの採用プロトコルは「世代ファイルの不在 = 削除が届いた」を前提にしており、
  ファイル作成自体が永続化される前の電源断（作成直後の孤児化・部分 tmp の誤採用）は守れない。
  SYNC モードはディレクトリ fsync（オープン直後 + rename 直後）でこの穴を塞ぎ、
  「追記が返った = 電源断まで耐える」の契約を完成させる。ASYNC モードは電源断契約が
  もともと緩い（直近数十秒を失いうる）ため、コストを避けて採用プロトコルのみで運用する
- ディレクトリ fsync の実装は環境で分かれる（機能差ではなく API の存在差）:
  Android 実機は android.system.Os（API 21+）、JVM ユニットテストは java.nio.file。
  どちらも同じ fsync(2) をディレクトリ fd に発行する。Os 実装は JVM で実行できないため
  カバレッジ対象外とし、実機挙動は Instrumentation テストで検証する
- 旧世代の削除はオープン時も「最新世代のオープンに成功してから」行う
  （最新世代が開けなかったとき、直前の世代を道連れにしない）
- 再 compaction の抑止: 「閾値超え かつ 直前の compaction 直後サイズの 2 倍以上」の
  二重条件。ライブデータ自体が閾値を超えているときのスラッシングを防ぐ
- rename 後も同じファイルハンドルで追記を続ける（rename はファイルの実体を変えない）
- プロセス内の追記競合は書き込みロックで直列化済み。一時停止フックは現時点では
  クラッシュ注入（電源断の決定的な再現）に使い、プロセス間競合の検証は
  マルチプロセス対応のスコープ

### 変更通知（リアクティビティ）

変更リスナーをコアプリミティブとし、リアクティブな上位 API は全部その上に載せる。
「更新内容を受け取れるリスナーさえあれば後でどうにでもなる」を設計原則にする。

- **リスナーの形**: `(key, newValue)` を受け取る。SharedPreferences の
  `OnSharedPreferenceChangeListener` がキーしか渡さない（値は再取得が必要）のに対し、
  値まで渡すことでリスナー内の読み出し競合を避ける
- **イベント源はジャーナル差分リプレイ**: 自プロセスの書き込みも他プロセスの書き込みも、
  「ジャーナルに追記されたエントリをキャッシュに適用する」という同じ経路を通る。
  この適用点でイベントを発火すれば、ローカル変更とプロセス跨ぎ変更が単一の仕組みで
  通知される。SharedPreferences が一度も持てなかったプロセス間変更通知が、
  マルチプロセス機構の副産物として手に入る
- **配送スレッド**: デフォルトは専用ディスパッチスレッド。UI 向けに main thread
  ディスパッチのオプションを用意。リスナー内で daybook を再操作してもデッドロックしない
  ことを保証する（配送はロック外で行う）
- **参照保持**: 明示的な register/unregister で強参照。SharedPreferences の
  WeakHashMap 保持（GC でリスナーが黙って消える有名な罠）は踏襲しない
- **互換リスナー**: `SharedPreferences` インターフェース実装の一部として
  `OnSharedPreferenceChangeListener` もサポート（ドロップイン移行のため）
- **上位 API**: Flow アダプタ（`fun <T> watch(key): Flow<T>` 相当）は別モジュールの
  薄いラッパーとして提供。コアは coroutine 非依存を保つ

### SharedPreferences との相互マイグレーション

導入（import）だけでなく撤退（export）も一級のスコープとする。
「試しに入れて、合わなければ無傷で戻れる」ことが採用障壁を下げる。

- **import**: 初回オープン時に旧 XML を読んでジャーナルに流し込むインポータを提供。
  `SharedPreferences` インターフェースを実装するため、呼び出し側は DI の差し替えのみ
- **export**: daybook の現在状態を `SharedPreferences` に書き戻すエクスポータを提供。
  自前で XML を書かず、公式 API（`edit()` + `commit()`）経由で書き戻すことで
  フォーマット互換の責任を負わない
- **型システムの制約**: 相互変換を保証するため、daybook の値型は SharedPreferences が
  サポートする型に揃える — `String` / `Int` / `Long` / `Float` / `Boolean` / `Set<String>`。
  独自型の追加（`ByteArray` 等）は往復可能性を壊すため、やるなら v2 以降で
  「export 不可」を型レベルで明示できる形を考えてから
- 移行の冪等性: import は「未取り込みのときだけ実行」のマーカーを持ち、
  再実行しても二重取り込みしない

## テスト戦略

「コアは JVM で網羅、実機は結合点だけ」の分離:

### JVM ユニットテスト（コアロジック全部）

- ジャーナルのリプレイ・CRC 検証・compaction・破損テール切り捨ては純粋なファイル IO。
  一時ディレクトリ + 壊れたジャーナルのフィクスチャで高速に回す
- fsync 層をインターフェースで抽象化し、「sync 前にクラッシュ」「追記途中でクラッシュ」を
  任意のバイト位置で決定的に注入する
- 注意: Robolectric は FileObserver（inotify）を実質サポートしない。
  変更検知まわりを JVM テストでカバーした気にならないこと

### Instrumentation テスト（結合点のみ）

- FileObserver の実挙動
- マルチプロセス同期: `android:process` 指定の Service を 2 つ立てて相互読み書き
- プロセスキル耐性: `am kill` / `Process.killProcess()` で再現
- `am kill` 系は CI のエミュレータで flaky になりがち。リトライ前提の別スイートに隔離する

## 将来の視野

- KMP 対応: ファイル IO だけ expect/actual で差し替えれば、純 Kotlin である利点が
  そのままポータビリティになる。ただし v1 では Android に集中する

## 名前について

会計の daybook（仕訳帳）から。「一次記録に追記 → 定期的に整理して元帳へ転記」という
会計のメタファーが、ジャーナル + compaction のアーキテクチャ説明そのものになっている。

## 先行例と比較メモ

| | 読み出し | 書き込み | マルチプロセス | 実装 |
|---|---|---|---|---|
| SharedPreferences | 初回ロードで同期ブロック | 全量 XML 書き換え + QueuedWork で ANR | 非対応（MODE_MULTI_PROCESS は deprecated かつ不完全） | Java |
| DataStore | Flow + coroutine 必須 | 全量書き換え | MultiProcessDataStoreFactory | Kotlin |
| MMKV | mmap 同期読み | 追記 + compaction | FileLock + ポーリング的検知 | C++ |
| Harmony | 同期読み | メイン + トランザクションファイル 2 層 | FileObserver ベース | Java |
| **daybook** | メモリ同期読み | 追記ジャーナル + CRC | ジャーナル差分リプレイ + FileObserver | 純 Kotlin |

Harmony の 2 層構成が必要だったのは元が全量書き換えモデルだから。
最初からジャーナルなら配信チャネルと永続化が一本化できる、というのが本設計の核。
